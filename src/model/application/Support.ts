export enum DbEngine {
  KNEX = 'knex'
}

export enum DbType {
  MYSQL = "mysql",
  POSTGRESQL = "postgresql",
  SQLITE3 = "sqlite3",
  MARIADB = "mariadb",
  ORACLE = "oracle"
}

export enum UI {
  REACT = 'react'
}

export enum Style {
  BOOTSTRAP = 'bootstrap'
}

export enum Controller {
  REST_EXPRESS = "restExpress"
}

export enum State {
  REACT_REDUX = "react-redux"
}

export enum I18N {
  REACT_INTL = "react-intl",
  RAW = "raw"
}
